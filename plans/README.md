# Implementation Plans

Generated by the improve skill on 2026-06-27. Commit `05a7783`.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Security Hardening (SEC-02, SEC-01, ERR-01, VAL-01, COR-01) | P1 | S | — | TODO |
| 002 | R2 Performance and Storage Correctness (PERF-02,04,05,06,07,08, RES-01) | P1 | M | — | TODO |
| 003 | Dependency Cleanup and Dead Code Removal (DEPS-01,02,03, ARCH-01,02, DX-01) | P1 | S | — | TODO |
| 004 | Test Coverage — R2 Backend and Repository CRUD (TEST-02, TEST-03) | P2 | M | — | TODO |
| 005 | DB Performance and CI Pipeline (PERF-01, ARCH-03, DX-03) | P2 | M | 002 | TODO |

## Dependency notes

- Plans 001, 002, 003, 004 are independent — they touch different files and can execute in parallel.
- Plan 005 depends on 002: both modify `src/server/repositories.ts`. Run the drift check in 005 against 002's output before executing.
- Plan 003 adds the `typecheck` script; 005's CI workflow uses it. Run 003 before 005 so the CI pipeline is valid from day one.

## Findings considered and rejected

- SECURITY-01 (perf agent): "R2 credentials read from env on every call" — not a finding; AWS SDK S3Client is designed to be instantiated with env vars. The actual concern (new instance per call) is captured in PERF-06 / plan 002.
- COR-03: setInterval in serverless — subsumed by ARCH-02 (the Map is never populated; deleting it also deletes the interval). Captured in plan 003.
- SEC-03 (writeRef TOCTOU race): real finding, deferred. R2 has no atomic CAS primitive; fixing this requires a distributed lock (Cloudflare Durable Object or a DB advisory lock). Scope is too large to include alongside plan 001. Track separately.
- COR-02 (GitTransaction.begin() no-op): real finding, deferred. Drizzle's transaction API does exist (`db.transaction()`), but wiring it into the GitTransaction class requires understanding all callers and a non-trivial refactor. Track separately.
- TEST-01 (zero coverage on git HTTP protocol): real finding. L effort — requires mocking `child_process.spawn` or spinning up a real git server. Deferred as a dedicated effort after the test infrastructure in plan 004 lands.
- DX-02 (no pre-commit hooks): real finding. Deferred — husky setup is mechanical but requires developer buy-in. Can be added after CI (plan 005) is confirmed working.
- CODEQ-02 (files.ts god module): valid but HIGH risk to split. Deferred until after test coverage lands.
- ARCH-04 (filesystem cache complexity): partially fixed by plan 002's PERF-03 note. Evaluate after plan 002 lands.
